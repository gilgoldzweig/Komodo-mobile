/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models

import ca.glong.komodo.core.openapi.models.DeploymentImage
import ca.glong.komodo.core.openapi.models.RestartMode
import ca.glong.komodo.core.openapi.models.TerminationSignal

import kotlinx.serialization.*
import kotlinx.serialization.descriptors.*
import kotlinx.serialization.encoding.*

/**
 * 
 *
 * @param autoUpdate Whether to automatically redeploy when newer a image is found. Will implicitly enable `poll_for_updates`, you don't need to enable both.
 * @param command This is interpolated at the end of the `docker run` command, which means they are either passed to the containers inner process, or replaces the container command, depending on use of ENTRYPOINT or CMD in dockerfile. Empty is no command.
 * @param environment The environment variables passed to the container / service.
 * @param extraArgs Extra args which are interpolated into the `docker run` / `docker service create` command, and affect the container configuration.  - Container ref: https://docs.docker.com/reference/cli/docker/container/run/#options - Swarm Service ref: https://docs.docker.com/reference/cli/docker/service/create/#options
 * @param image The image which the deployment deploys. Can either be a user inputted image, or a Komodo Build.
 * @param imageRegistryAccount Configure the account used to pull the image from the registry. Used with `docker login`.   - If the field is empty string, will use the same account config as the build, or none at all if using image.  - If the field contains an account, a token for the account must be available.  - Will get the registry domain from the build / image
 * @param labels The docker labels given to the container.
 * @param links Configure quick links that are displayed in the resource header
 * @param network The network attached to the container. Default is `host`.
 * @param pollForUpdates Whether to poll for any updates to the image.
 * @param ports The container port mapping. Irrelevant if container network is `host`. Maps ports on host to ports on container.
 * @param redeployOnBuild Whether to redeploy the deployment whenever the attached build finishes.
 * @param replicas The number of replicas for the Service.  Note. Only used in Swarm mode.
 * @param restart The restart mode given to the container.
 * @param sendAlerts Whether to send ContainerStateChange alerts for this deployment.
 * @param serverId The Server to deploy the Deployment on, setting the Deployment into Container mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Deployment will be in Swarm mode.
 * @param skipSecretInterp Whether to skip secret interpolation into the deployment environment variables.
 * @param swarmId The Swarm to deploy the Deployment on (as a Swarm Service), setting the Deployment into Swarm mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Deployment will be in Swarm mode.
 * @param termSignalLabels Labels attached to various termination signal options. Used to specify different shutdown functionality depending on the termination signal.
 * @param terminationSignal The default termination signal to use to stop the deployment. Defaults to SigTerm (default docker signal).
 * @param terminationTimeout The termination timeout.
 * @param volumes The container volume mapping. Maps files / folders on host to files / folders in container.
 */
@Serializable@Serializable

data class DeploymentConfig (

    /* Whether to automatically redeploy when newer a image is found. Will implicitly enable `poll_for_updates`, you don't need to enable both. */
    @SerialName(value = "auto_update") val autoUpdate: kotlin.Boolean? = null,

    /* This is interpolated at the end of the `docker run` command, which means they are either passed to the containers inner process, or replaces the container command, depending on use of ENTRYPOINT or CMD in dockerfile. Empty is no command. */
    @SerialName(value = "command") val command: kotlin.String? = null,

    /* The environment variables passed to the container / service. */
    @SerialName(value = "environment") val environment: kotlin.String? = null,

    /* Extra args which are interpolated into the `docker run` / `docker service create` command, and affect the container configuration.  - Container ref: https://docs.docker.com/reference/cli/docker/container/run/#options - Swarm Service ref: https://docs.docker.com/reference/cli/docker/service/create/#options */
    @SerialName(value = "extra_args") val extraArgs: kotlin.collections.List<kotlin.String>? = null,

    /* The image which the deployment deploys. Can either be a user inputted image, or a Komodo Build. */
    @SerialName(value = "image") val image: DeploymentImage? = null,

    /* Configure the account used to pull the image from the registry. Used with `docker login`.   - If the field is empty string, will use the same account config as the build, or none at all if using image.  - If the field contains an account, a token for the account must be available.  - Will get the registry domain from the build / image */
    @SerialName(value = "image_registry_account") val imageRegistryAccount: kotlin.String? = null,

    /* The docker labels given to the container. */
    @SerialName(value = "labels") val labels: kotlin.String? = null,

    /* Configure quick links that are displayed in the resource header */
    @SerialName(value = "links") val links: kotlin.collections.List<kotlin.String>? = null,

    /* The network attached to the container. Default is `host`. */
    @SerialName(value = "network") val network: kotlin.String? = null,

    /* Whether to poll for any updates to the image. */
    @SerialName(value = "poll_for_updates") val pollForUpdates: kotlin.Boolean? = null,

    /* The container port mapping. Irrelevant if container network is `host`. Maps ports on host to ports on container. */
    @SerialName(value = "ports") val ports: kotlin.String? = null,

    /* Whether to redeploy the deployment whenever the attached build finishes. */
    @SerialName(value = "redeploy_on_build") val redeployOnBuild: kotlin.Boolean? = null,

    /* The number of replicas for the Service.  Note. Only used in Swarm mode. */
    @SerialName(value = "replicas") val replicas: kotlin.Int? = null,

    /* The restart mode given to the container. */
    @SerialName(value = "restart") val restart: RestartMode? = null,

    /* Whether to send ContainerStateChange alerts for this deployment. */
    @SerialName(value = "send_alerts") val sendAlerts: kotlin.Boolean? = null,

    /* The Server to deploy the Deployment on, setting the Deployment into Container mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Deployment will be in Swarm mode. */
    @SerialName(value = "server_id") val serverId: kotlin.String? = null,

    /* Whether to skip secret interpolation into the deployment environment variables. */
    @SerialName(value = "skip_secret_interp") val skipSecretInterp: kotlin.Boolean? = null,

    /* The Swarm to deploy the Deployment on (as a Swarm Service), setting the Deployment into Swarm mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Deployment will be in Swarm mode. */
    @SerialName(value = "swarm_id") val swarmId: kotlin.String? = null,

    /* Labels attached to various termination signal options. Used to specify different shutdown functionality depending on the termination signal. */
    @SerialName(value = "term_signal_labels") val termSignalLabels: kotlin.String? = null,

    /* The default termination signal to use to stop the deployment. Defaults to SigTerm (default docker signal). */
    @SerialName(value = "termination_signal") val terminationSignal: TerminationSignal? = null,

    /* The termination timeout. */
    @SerialName(value = "termination_timeout") val terminationTimeout: kotlin.Int? = null,

    /* The container volume mapping. Maps files / folders on host to files / folders in container. */
    @SerialName(value = "volumes") val volumes: kotlin.String? = null

) {


}

