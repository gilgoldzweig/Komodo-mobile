/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models

import io.kotlintest.shouldBe
import io.kotlintest.specs.ShouldSpec

import ca.glong.komodo.core.openapi.models.HostConfig
import ca.glong.komodo.core.openapi.models.DeviceMapping
import ca.glong.komodo.core.openapi.models.DeviceRequest
import ca.glong.komodo.core.openapi.models.HostConfigCgroupnsModeEnum
import ca.glong.komodo.core.openapi.models.HostConfigIsolationEnum
import ca.glong.komodo.core.openapi.models.HostConfigLogConfig
import ca.glong.komodo.core.openapi.models.Mount
import ca.glong.komodo.core.openapi.models.PortBinding
import ca.glong.komodo.core.openapi.models.ResourcesBlkioWeightDevice
import ca.glong.komodo.core.openapi.models.ResourcesUlimits
import ca.glong.komodo.core.openapi.models.RestartPolicy
import ca.glong.komodo.core.openapi.models.ThrottleDevice

class HostConfigTest : ShouldSpec() {
    init {
        // uncomment below to create an instance of HostConfig
        //val modelInstance = HostConfig()

        // to test the property `annotations` - Arbitrary non-identifying metadata attached to container and provided to the runtime when the container is started.
        should("test annotations") {
            // uncomment below to test the property
            //modelInstance.annotations shouldBe ("TODO")
        }

        // to test the property `autoRemove` - Automatically remove the container when the container's process exits. This has no effect if `RestartPolicy` is set.
        should("test autoRemove") {
            // uncomment below to test the property
            //modelInstance.autoRemove shouldBe ("TODO")
        }

        // to test the property `binds` - A list of volume bindings for this container. Each volume binding is a string in one of these forms:  - `host-src:container-dest[:options]` to bind-mount a host path   into the container. Both `host-src`, and `container-dest` must   be an _absolute_ path. - `volume-name:container-dest[:options]` to bind-mount a volume   managed by a volume driver into the container. `container-dest`   must be an _absolute_ path.  `options` is an optional, comma-delimited list of:  - `nocopy` disables automatic copying of data from the container   path to the volume. The `nocopy` flag only applies to named volumes. - `[ro|rw]` mounts a volume read-only or read-write, respectively.   If omitted or set to `rw`, volumes are mounted read-write. - `[z|Z]` applies SELinux labels to allow or deny multiple containers   to read and write to the same volume.     - `z`: a _shared_ content label is applied to the content. This       label indicates that multiple containers can share the volume       content, for both reading and writing.     - `Z`: a _private unshared_ label is applied to the content.       This label indicates that only the current container can use       a private volume. Labeling systems such as SELinux require       proper labels to be placed on volume content that is mounted       into a container. Without a label, the security system can       prevent a container's processes from using the content. By       default, the labels set by the host operating system are not       modified. - `[[r]shared|[r]slave|[r]private]` specifies mount   [propagation behavior](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt).   This only applies to bind-mounted volumes, not internal volumes   or named volumes. Mount propagation requires the source mount   point (the location where the source directory is mounted in the   host operating system) to have the correct propagation properties.   For shared volumes, the source mount point must be set to `shared`.   For slave volumes, the mount must be set to either `shared` or   `slave`.
        should("test binds") {
            // uncomment below to test the property
            //modelInstance.binds shouldBe ("TODO")
        }

        // to test the property `blkioDeviceReadBps` - Limit read rate (bytes per second) from a device, in the form:  ``` [{\\\"Path\\\": \\\"device_path\\\", \\\"Rate\\\": rate}] ```
        should("test blkioDeviceReadBps") {
            // uncomment below to test the property
            //modelInstance.blkioDeviceReadBps shouldBe ("TODO")
        }

        // to test the property `blkioDeviceReadIOps` - Limit read rate (IO per second) from a device, in the form:  ``` [{\\\"Path\\\": \\\"device_path\\\", \\\"Rate\\\": rate}] ```
        should("test blkioDeviceReadIOps") {
            // uncomment below to test the property
            //modelInstance.blkioDeviceReadIOps shouldBe ("TODO")
        }

        // to test the property `blkioDeviceWriteBps` - Limit write rate (bytes per second) to a device, in the form:  ``` [{\\\"Path\\\": \\\"device_path\\\", \\\"Rate\\\": rate}] ```
        should("test blkioDeviceWriteBps") {
            // uncomment below to test the property
            //modelInstance.blkioDeviceWriteBps shouldBe ("TODO")
        }

        // to test the property `blkioDeviceWriteIOps` - Limit write rate (IO per second) to a device, in the form:  ``` [{\\\"Path\\\": \\\"device_path\\\", \\\"Rate\\\": rate}] ```
        should("test blkioDeviceWriteIOps") {
            // uncomment below to test the property
            //modelInstance.blkioDeviceWriteIOps shouldBe ("TODO")
        }

        // to test the property `blkioWeight` - Block IO weight (relative weight).
        should("test blkioWeight") {
            // uncomment below to test the property
            //modelInstance.blkioWeight shouldBe ("TODO")
        }

        // to test the property `blkioWeightDevice` - Block IO weight (relative device weight) in the form:  ``` [{\\\"Path\\\": \\\"device_path\\\", \\\"Weight\\\": weight}] ```
        should("test blkioWeightDevice") {
            // uncomment below to test the property
            //modelInstance.blkioWeightDevice shouldBe ("TODO")
        }

        // to test the property `capAdd` - A list of kernel capabilities to add to the container. Conflicts with option 'Capabilities'.
        should("test capAdd") {
            // uncomment below to test the property
            //modelInstance.capAdd shouldBe ("TODO")
        }

        // to test the property `capDrop` - A list of kernel capabilities to drop from the container. Conflicts with option 'Capabilities'.
        should("test capDrop") {
            // uncomment below to test the property
            //modelInstance.capDrop shouldBe ("TODO")
        }

        // to test the property `cgroup` - Cgroup to use for the container.
        should("test cgroup") {
            // uncomment below to test the property
            //modelInstance.cgroup shouldBe ("TODO")
        }

        // to test the property `cgroupParent` - Path to `cgroups` under which the container's `cgroup` is created. If the path is not absolute, the path is considered to be relative to the `cgroups` path of the init process. Cgroups are created if they do not already exist.
        should("test cgroupParent") {
            // uncomment below to test the property
            //modelInstance.cgroupParent shouldBe ("TODO")
        }

        // to test the property `cgroupnsMode` - cgroup namespace mode for the container. Possible values are:  - `\\\"private\\\"`: the container runs in its own private cgroup namespace - `\\\"host\\\"`: use the host system's cgroup namespace  If not specified, the daemon default is used, which can either be `\\\"private\\\"` or `\\\"host\\\"`, depending on daemon version, kernel support and configuration.
        should("test cgroupnsMode") {
            // uncomment below to test the property
            //modelInstance.cgroupnsMode shouldBe ("TODO")
        }

        // to test the property `consoleSize` - Initial console size, as an `[height, width]` array.
        should("test consoleSize") {
            // uncomment below to test the property
            //modelInstance.consoleSize shouldBe ("TODO")
        }

        // to test the property `containerIDFile` - Path to a file where the container ID is written
        should("test containerIDFile") {
            // uncomment below to test the property
            //modelInstance.containerIDFile shouldBe ("TODO")
        }

        // to test the property `cpuCount` - The number of usable CPUs (Windows only).  On Windows Server containers, the processor resource controls are mutually exclusive. The order of precedence is `CPUCount` first, then `CPUShares`, and `CPUPercent` last.
        should("test cpuCount") {
            // uncomment below to test the property
            //modelInstance.cpuCount shouldBe ("TODO")
        }

        // to test the property `cpuPercent` - The usable percentage of the available CPUs (Windows only).  On Windows Server containers, the processor resource controls are mutually exclusive. The order of precedence is `CPUCount` first, then `CPUShares`, and `CPUPercent` last.
        should("test cpuPercent") {
            // uncomment below to test the property
            //modelInstance.cpuPercent shouldBe ("TODO")
        }

        // to test the property `cpuPeriod` - The length of a CPU period in microseconds.
        should("test cpuPeriod") {
            // uncomment below to test the property
            //modelInstance.cpuPeriod shouldBe ("TODO")
        }

        // to test the property `cpuQuota` - Microseconds of CPU time that the container can get in a CPU period.
        should("test cpuQuota") {
            // uncomment below to test the property
            //modelInstance.cpuQuota shouldBe ("TODO")
        }

        // to test the property `cpuRealtimePeriod` - The length of a CPU real-time period in microseconds. Set to 0 to allocate no time allocated to real-time tasks.
        should("test cpuRealtimePeriod") {
            // uncomment below to test the property
            //modelInstance.cpuRealtimePeriod shouldBe ("TODO")
        }

        // to test the property `cpuRealtimeRuntime` - The length of a CPU real-time runtime in microseconds. Set to 0 to allocate no time allocated to real-time tasks.
        should("test cpuRealtimeRuntime") {
            // uncomment below to test the property
            //modelInstance.cpuRealtimeRuntime shouldBe ("TODO")
        }

        // to test the property `cpuShares` - An integer value representing this container's relative CPU weight versus other containers.
        should("test cpuShares") {
            // uncomment below to test the property
            //modelInstance.cpuShares shouldBe ("TODO")
        }

        // to test the property `cpusetCpus` - CPUs in which to allow execution (e.g., `0-3`, `0,1`).
        should("test cpusetCpus") {
            // uncomment below to test the property
            //modelInstance.cpusetCpus shouldBe ("TODO")
        }

        // to test the property `cpusetMems` - Memory nodes (MEMs) in which to allow execution (0-3, 0,1). Only effective on NUMA systems.
        should("test cpusetMems") {
            // uncomment below to test the property
            //modelInstance.cpusetMems shouldBe ("TODO")
        }

        // to test the property `deviceCgroupRules` - a list of cgroup rules to apply to the container
        should("test deviceCgroupRules") {
            // uncomment below to test the property
            //modelInstance.deviceCgroupRules shouldBe ("TODO")
        }

        // to test the property `deviceRequests` - A list of requests for devices to be sent to device drivers.
        should("test deviceRequests") {
            // uncomment below to test the property
            //modelInstance.deviceRequests shouldBe ("TODO")
        }

        // to test the property `devices` - A list of devices to add to the container.
        should("test devices") {
            // uncomment below to test the property
            //modelInstance.devices shouldBe ("TODO")
        }

        // to test the property `dns` - A list of DNS servers for the container to use.
        should("test dns") {
            // uncomment below to test the property
            //modelInstance.dns shouldBe ("TODO")
        }

        // to test the property `dnsOptions` - A list of DNS options.
        should("test dnsOptions") {
            // uncomment below to test the property
            //modelInstance.dnsOptions shouldBe ("TODO")
        }

        // to test the property `dnsSearch` - A list of DNS search domains.
        should("test dnsSearch") {
            // uncomment below to test the property
            //modelInstance.dnsSearch shouldBe ("TODO")
        }

        // to test the property `extraHosts` - A list of hostnames/IP mappings to add to the container's `/etc/hosts` file. Specified in the form `[\\\"hostname:IP\\\"]`.
        should("test extraHosts") {
            // uncomment below to test the property
            //modelInstance.extraHosts shouldBe ("TODO")
        }

        // to test the property `groupAdd` - A list of additional groups that the container process will run as.
        should("test groupAdd") {
            // uncomment below to test the property
            //modelInstance.groupAdd shouldBe ("TODO")
        }

        // to test the property `ioMaximumBandwidth` - Maximum IO in bytes per second for the container system drive (Windows only).
        should("test ioMaximumBandwidth") {
            // uncomment below to test the property
            //modelInstance.ioMaximumBandwidth shouldBe ("TODO")
        }

        // to test the property `ioMaximumIOps` - Maximum IOps for the container system drive (Windows only)
        should("test ioMaximumIOps") {
            // uncomment below to test the property
            //modelInstance.ioMaximumIOps shouldBe ("TODO")
        }

        // to test the property ``init`` - Run an init inside the container that forwards signals and reaps processes. This field is omitted if empty, and the default (as configured on the daemon) is used.
        should("test `init`") {
            // uncomment below to test the property
            //modelInstance.`init` shouldBe ("TODO")
        }

        // to test the property `ipcMode` - IPC sharing mode for the container. Possible values are:  - `\\\"none\\\"`: own private IPC namespace, with /dev/shm not mounted - `\\\"private\\\"`: own private IPC namespace - `\\\"shareable\\\"`: own private IPC namespace, with a possibility to share it with other containers - `\\\"container:<name|id>\\\"`: join another (shareable) container's IPC namespace - `\\\"host\\\"`: use the host system's IPC namespace  If not specified, daemon default is used, which can either be `\\\"private\\\"` or `\\\"shareable\\\"`, depending on daemon version and configuration.
        should("test ipcMode") {
            // uncomment below to test the property
            //modelInstance.ipcMode shouldBe ("TODO")
        }

        // to test the property `isolation` - Isolation technology of the container. (Windows only)
        should("test isolation") {
            // uncomment below to test the property
            //modelInstance.isolation shouldBe ("TODO")
        }

        // to test the property `links` - A list of links for the container in the form `container_name:alias`.
        should("test links") {
            // uncomment below to test the property
            //modelInstance.links shouldBe ("TODO")
        }

        // to test the property `logConfig`
        should("test logConfig") {
            // uncomment below to test the property
            //modelInstance.logConfig shouldBe ("TODO")
        }

        // to test the property `maskedPaths` - The list of paths to be masked inside the container (this overrides the default set of paths).
        should("test maskedPaths") {
            // uncomment below to test the property
            //modelInstance.maskedPaths shouldBe ("TODO")
        }

        // to test the property `memory` - Memory limit in bytes.
        should("test memory") {
            // uncomment below to test the property
            //modelInstance.memory shouldBe ("TODO")
        }

        // to test the property `memoryReservation` - Memory soft limit in bytes.
        should("test memoryReservation") {
            // uncomment below to test the property
            //modelInstance.memoryReservation shouldBe ("TODO")
        }

        // to test the property `memorySwap` - Total memory limit (memory + swap). Set as `-1` to enable unlimited swap.
        should("test memorySwap") {
            // uncomment below to test the property
            //modelInstance.memorySwap shouldBe ("TODO")
        }

        // to test the property `memorySwappiness` - Tune a container's memory swappiness behavior. Accepts an integer between 0 and 100.
        should("test memorySwappiness") {
            // uncomment below to test the property
            //modelInstance.memorySwappiness shouldBe ("TODO")
        }

        // to test the property `mounts` - Specification for mounts to be added to the container.
        should("test mounts") {
            // uncomment below to test the property
            //modelInstance.mounts shouldBe ("TODO")
        }

        // to test the property `nanoCpus` - CPU quota in units of 10<sup>-9</sup> CPUs.
        should("test nanoCpus") {
            // uncomment below to test the property
            //modelInstance.nanoCpus shouldBe ("TODO")
        }

        // to test the property `networkMode` - Network mode to use for this container. Supported standard values are: `bridge`, `host`, `none`, and `container:<name|id>`. Any other value is taken as a custom network's name to which this container should connect to.
        should("test networkMode") {
            // uncomment below to test the property
            //modelInstance.networkMode shouldBe ("TODO")
        }

        // to test the property `oomKillDisable` - Disable OOM Killer for the container.
        should("test oomKillDisable") {
            // uncomment below to test the property
            //modelInstance.oomKillDisable shouldBe ("TODO")
        }

        // to test the property `oomScoreAdj` - An integer value containing the score given to the container in order to tune OOM killer preferences.
        should("test oomScoreAdj") {
            // uncomment below to test the property
            //modelInstance.oomScoreAdj shouldBe ("TODO")
        }

        // to test the property `pidMode` - Set the PID (Process) Namespace mode for the container. It can be either:  - `\\\"container:<name|id>\\\"`: joins another container's PID namespace - `\\\"host\\\"`: use the host's PID namespace inside the container
        should("test pidMode") {
            // uncomment below to test the property
            //modelInstance.pidMode shouldBe ("TODO")
        }

        // to test the property `pidsLimit` - Tune a container's PIDs limit. Set `0` or `-1` for unlimited, or `null` to not change.
        should("test pidsLimit") {
            // uncomment below to test the property
            //modelInstance.pidsLimit shouldBe ("TODO")
        }

        // to test the property `portBindings`
        should("test portBindings") {
            // uncomment below to test the property
            //modelInstance.portBindings shouldBe ("TODO")
        }

        // to test the property `privileged` - Gives the container full access to the host.
        should("test privileged") {
            // uncomment below to test the property
            //modelInstance.privileged shouldBe ("TODO")
        }

        // to test the property `publishAllPorts` - Allocates an ephemeral host port for all of a container's exposed ports.  Ports are de-allocated when the container stops and allocated when the container starts. The allocated port might be changed when restarting the container.  The port is selected from the ephemeral port range that depends on the kernel. For example, on Linux the range is defined by `/proc/sys/net/ipv4/ip_local_port_range`.
        should("test publishAllPorts") {
            // uncomment below to test the property
            //modelInstance.publishAllPorts shouldBe ("TODO")
        }

        // to test the property `readonlyPaths` - The list of paths to be set as read-only inside the container (this overrides the default set of paths).
        should("test readonlyPaths") {
            // uncomment below to test the property
            //modelInstance.readonlyPaths shouldBe ("TODO")
        }

        // to test the property `readonlyRootfs` - Mount the container's root filesystem as read only.
        should("test readonlyRootfs") {
            // uncomment below to test the property
            //modelInstance.readonlyRootfs shouldBe ("TODO")
        }

        // to test the property `restartPolicy`
        should("test restartPolicy") {
            // uncomment below to test the property
            //modelInstance.restartPolicy shouldBe ("TODO")
        }

        // to test the property `runtime` - Runtime to use with this container.
        should("test runtime") {
            // uncomment below to test the property
            //modelInstance.runtime shouldBe ("TODO")
        }

        // to test the property `securityOpt` - A list of string values to customize labels for MLS systems, such as SELinux.
        should("test securityOpt") {
            // uncomment below to test the property
            //modelInstance.securityOpt shouldBe ("TODO")
        }

        // to test the property `shmSize` - Size of `/dev/shm` in bytes. If omitted, the system uses 64MB.
        should("test shmSize") {
            // uncomment below to test the property
            //modelInstance.shmSize shouldBe ("TODO")
        }

        // to test the property `storageOpt` - Storage driver options for this container, in the form `{\\\"size\\\": \\\"120G\\\"}`.
        should("test storageOpt") {
            // uncomment below to test the property
            //modelInstance.storageOpt shouldBe ("TODO")
        }

        // to test the property `sysctls` - A list of kernel parameters (sysctls) to set in the container. For example:  ``` {\\\"net.ipv4.ip_forward\\\": \\\"1\\\"} ```
        should("test sysctls") {
            // uncomment below to test the property
            //modelInstance.sysctls shouldBe ("TODO")
        }

        // to test the property `tmpfs` - A map of container directories which should be replaced by tmpfs mounts, and their corresponding mount options. For example:  ``` { \\\"/run\\\": \\\"rw,noexec,nosuid,size=65536k\\\" } ```
        should("test tmpfs") {
            // uncomment below to test the property
            //modelInstance.tmpfs shouldBe ("TODO")
        }

        // to test the property `utSMode` - UTS namespace to use for the container.
        should("test utSMode") {
            // uncomment below to test the property
            //modelInstance.utSMode shouldBe ("TODO")
        }

        // to test the property `ulimits` - A list of resource limits to set in the container. For example:  ``` {\\\"Name\\\": \\\"nofile\\\", \\\"Soft\\\": 1024, \\\"Hard\\\": 2048} ```
        should("test ulimits") {
            // uncomment below to test the property
            //modelInstance.ulimits shouldBe ("TODO")
        }

        // to test the property `usernsMode` - Sets the usernamespace mode for the container when usernamespace remapping option is enabled.
        should("test usernsMode") {
            // uncomment below to test the property
            //modelInstance.usernsMode shouldBe ("TODO")
        }

        // to test the property `volumeDriver` - Driver that this container uses to mount volumes.
        should("test volumeDriver") {
            // uncomment below to test the property
            //modelInstance.volumeDriver shouldBe ("TODO")
        }

        // to test the property `volumesFrom` - A list of volumes to inherit from another container, specified in the form `<container name>[:<ro|rw>]`.
        should("test volumesFrom") {
            // uncomment below to test the property
            //modelInstance.volumesFrom shouldBe ("TODO")
        }

    }
}
