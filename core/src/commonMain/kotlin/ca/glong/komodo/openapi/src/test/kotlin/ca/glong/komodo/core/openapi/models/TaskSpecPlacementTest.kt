/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models

import io.kotlintest.shouldBe
import io.kotlintest.specs.ShouldSpec

import ca.glong.komodo.core.openapi.models.TaskSpecPlacement
import ca.glong.komodo.core.openapi.models.Platform
import ca.glong.komodo.core.openapi.models.TaskSpecPlacementPreferences

class TaskSpecPlacementTest : ShouldSpec() {
    init {
        // uncomment below to create an instance of TaskSpecPlacement
        //val modelInstance = TaskSpecPlacement()

        // to test the property `constraints` - An array of constraint expressions to limit the set of nodes where a task can be scheduled. Constraint expressions can either use a _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find nodes that satisfy every expression (AND match). Constraints can match node or Docker Engine labels as follows:  node attribute       | matches                        | example ---------------------|--------------------------------|----------------------------------------------- `node.id`            | Node ID                        | `node.id==2ivku8v2gvtg4` `node.hostname`      | Node hostname                  | `node.hostname!=node-2` `node.role`          | Node role (`manager`/`worker`) | `node.role==manager` `node.platform.os`   | Node operating system          | `node.platform.os==windows` `node.platform.arch` | Node architecture              | `node.platform.arch==x86_64` `node.labels`        | User-defined node labels       | `node.labels.security==high` `engine.labels`      | Docker Engine's labels         | `engine.labels.operatingsystem==ubuntu-24.04`  `engine.labels` apply to Docker Engine labels like operating system, drivers, etc. Swarm administrators add `node.labels` for operational purposes by using the [`node update endpoint`](#operation/NodeUpdate).
        should("test constraints") {
            // uncomment below to test the property
            //modelInstance.constraints shouldBe ("TODO")
        }

        // to test the property `maxReplicas` - Maximum number of replicas for per node (default value is 0, which is unlimited)
        should("test maxReplicas") {
            // uncomment below to test the property
            //modelInstance.maxReplicas shouldBe ("TODO")
        }

        // to test the property `platforms` - Platforms stores all the platforms that the service's image can run on. This field is used in the platform filter for scheduling. If empty, then the platform filter is off, meaning there are no scheduling restrictions.
        should("test platforms") {
            // uncomment below to test the property
            //modelInstance.platforms shouldBe ("TODO")
        }

        // to test the property `preferences` - Preferences provide a way to make the scheduler aware of factors such as topology. They are provided in order from highest to lowest precedence.
        should("test preferences") {
            // uncomment below to test the property
            //modelInstance.preferences shouldBe ("TODO")
        }

    }
}
