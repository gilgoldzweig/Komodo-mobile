/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models

import io.kotlintest.shouldBe
import io.kotlintest.specs.ShouldSpec

import ca.glong.komodo.core.openapi.models.Image
import ca.glong.komodo.core.openapi.models.GraphDriverData
import ca.glong.komodo.core.openapi.models.ImageConfig
import ca.glong.komodo.core.openapi.models.ImageInspectMetadata
import ca.glong.komodo.core.openapi.models.ImageInspectRootFs
import ca.glong.komodo.core.openapi.models.ImageManifestSummary
import ca.glong.komodo.core.openapi.models.OciDescriptor

class ImageTest : ShouldSpec() {
    init {
        // uncomment below to create an instance of Image
        //val modelInstance = Image()

        // to test the property `architecture` - Hardware CPU architecture that the image runs on.
        should("test architecture") {
            // uncomment below to test the property
            //modelInstance.architecture shouldBe ("TODO")
        }

        // to test the property `author` - Name of the author that was specified when committing the image, or as specified through MAINTAINER (deprecated) in the Dockerfile.
        should("test author") {
            // uncomment below to test the property
            //modelInstance.author shouldBe ("TODO")
        }

        // to test the property `comment` - Optional message that was set when committing or importing the image.
        should("test comment") {
            // uncomment below to test the property
            //modelInstance.comment shouldBe ("TODO")
        }

        // to test the property `config`
        should("test config") {
            // uncomment below to test the property
            //modelInstance.config shouldBe ("TODO")
        }

        // to test the property `created` - Date and time at which the image was created, formatted in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.  This information is only available if present in the image, and omitted otherwise.
        should("test created") {
            // uncomment below to test the property
            //modelInstance.created shouldBe ("TODO")
        }

        // to test the property `descriptor` - Descriptor is an OCI descriptor of the image target. In case of a multi-platform image, this descriptor points to the OCI index or a manifest list.  This field is only present if the daemon provides a multi-platform image store.  WARNING: This is experimental and may change at any time without any backward compatibility.
        should("test descriptor") {
            // uncomment below to test the property
            //modelInstance.descriptor shouldBe ("TODO")
        }

        // to test the property `graphDriver`
        should("test graphDriver") {
            // uncomment below to test the property
            //modelInstance.graphDriver shouldBe ("TODO")
        }

        // to test the property `id` - ID is the content-addressable ID of an image.  This identifier is a content-addressable digest calculated from the image's configuration (which includes the digests of layers used by the image).  Note that this digest differs from the `RepoDigests` below, which holds digests of image manifests that reference the image.
        should("test id") {
            // uncomment below to test the property
            //modelInstance.id shouldBe ("TODO")
        }

        // to test the property `manifests` - Manifests is a list of image manifests available in this image. It provides a more detailed view of the platform-specific image manifests or other image-attached data like build attestations.  Only available if the daemon provides a multi-platform image store and the `manifests` option is set in the inspect request.  WARNING: This is experimental and may change at any time without any backward compatibility.
        should("test manifests") {
            // uncomment below to test the property
            //modelInstance.manifests shouldBe ("TODO")
        }

        // to test the property `metadata`
        should("test metadata") {
            // uncomment below to test the property
            //modelInstance.metadata shouldBe ("TODO")
        }

        // to test the property `os` - Operating System the image is built to run on.
        should("test os") {
            // uncomment below to test the property
            //modelInstance.os shouldBe ("TODO")
        }

        // to test the property `osVersion` - Operating System version the image is built to run on (especially for Windows).
        should("test osVersion") {
            // uncomment below to test the property
            //modelInstance.osVersion shouldBe ("TODO")
        }

        // to test the property `repoDigests` - List of content-addressable digests of locally available image manifests that the image is referenced from. Multiple manifests can refer to the same image.  These digests are usually only available if the image was either pulled from a registry, or if the image was pushed to a registry, which is when the manifest is generated and its digest calculated.
        should("test repoDigests") {
            // uncomment below to test the property
            //modelInstance.repoDigests shouldBe ("TODO")
        }

        // to test the property `repoTags` - List of image names/tags in the local image cache that reference this image.  Multiple image tags can refer to the same image, and this list may be empty if no tags reference the image, in which case the image is \\\"untagged\\\", in which case it can still be referenced by its ID.
        should("test repoTags") {
            // uncomment below to test the property
            //modelInstance.repoTags shouldBe ("TODO")
        }

        // to test the property `rootFS`
        should("test rootFS") {
            // uncomment below to test the property
            //modelInstance.rootFS shouldBe ("TODO")
        }

        // to test the property `propertySize` - Total size of the image including all layers it is composed of.
        should("test propertySize") {
            // uncomment below to test the property
            //modelInstance.propertySize shouldBe ("TODO")
        }

        // to test the property `variant` - CPU architecture variant (presently ARM-only).
        should("test variant") {
            // uncomment below to test the property
            //modelInstance.variant shouldBe ("TODO")
        }

    }
}
