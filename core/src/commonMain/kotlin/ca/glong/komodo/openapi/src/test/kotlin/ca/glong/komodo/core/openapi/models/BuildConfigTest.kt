/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models

import io.kotlintest.shouldBe
import io.kotlintest.specs.ShouldSpec

import ca.glong.komodo.core.openapi.models.BuildConfig
import ca.glong.komodo.core.openapi.models.ImageRegistryConfig
import ca.glong.komodo.core.openapi.models.SystemCommand
import ca.glong.komodo.core.openapi.models.Version

class BuildConfigTest : ShouldSpec() {
    init {
        // uncomment below to create an instance of BuildConfig
        //val modelInstance = BuildConfig()

        // to test the property `autoIncrementVersion` - Whether to automatically increment the patch on every build. Default is `true`
        should("test autoIncrementVersion") {
            // uncomment below to test the property
            //modelInstance.autoIncrementVersion shouldBe ("TODO")
        }

        // to test the property `branch` - The branch of the repo.
        should("test branch") {
            // uncomment below to test the property
            //modelInstance.branch shouldBe ("TODO")
        }

        // to test the property `buildArgs` - Docker build arguments.  These values are visible in the final image by running `docker inspect`.
        should("test buildArgs") {
            // uncomment below to test the property
            //modelInstance.buildArgs shouldBe ("TODO")
        }

        // to test the property `buildPath` - The path of the docker build context relative to the root of the repo. Default: \".\" (the root of the repo).
        should("test buildPath") {
            // uncomment below to test the property
            //modelInstance.buildPath shouldBe ("TODO")
        }

        // to test the property `builderId` - Which builder is used to build the image.
        should("test builderId") {
            // uncomment below to test the property
            //modelInstance.builderId shouldBe ("TODO")
        }

        // to test the property `commit` - Optionally set a specific commit hash.
        should("test commit") {
            // uncomment below to test the property
            //modelInstance.commit shouldBe ("TODO")
        }

        // to test the property `dockerfile` - UI defined dockerfile contents. Supports variable / secret interpolation.
        should("test dockerfile") {
            // uncomment below to test the property
            //modelInstance.dockerfile shouldBe ("TODO")
        }

        // to test the property `dockerfilePath` - The path of the dockerfile relative to the build path.
        should("test dockerfilePath") {
            // uncomment below to test the property
            //modelInstance.dockerfilePath shouldBe ("TODO")
        }

        // to test the property `extraArgs` - Any extra docker cli arguments to be included in the build command
        should("test extraArgs") {
            // uncomment below to test the property
            //modelInstance.extraArgs shouldBe ("TODO")
        }

        // to test the property `filesOnHost` - If this is checked, the build will source the files on the host. Use `build_path` and `dockerfile_path` to specify the path on the host. This is useful for those who wish to setup their files on the host, rather than defining the contents in UI or in a git repo.
        should("test filesOnHost") {
            // uncomment below to test the property
            //modelInstance.filesOnHost shouldBe ("TODO")
        }

        // to test the property `gitAccount` - The git account used to access private repos. Passing empty string can only clone public repos.  Note. A token for the account must be available in the core config or the builder server's periphery config for the configured git provider.
        should("test gitAccount") {
            // uncomment below to test the property
            //modelInstance.gitAccount shouldBe ("TODO")
        }

        // to test the property `gitHttps` - Whether to use https to clone the repo (versus http). Default: true  Note. Komodo does not currently support cloning repos via ssh.
        should("test gitHttps") {
            // uncomment below to test the property
            //modelInstance.gitHttps shouldBe ("TODO")
        }

        // to test the property `gitProvider` - The git provider domain. Default: github.com
        should("test gitProvider") {
            // uncomment below to test the property
            //modelInstance.gitProvider shouldBe ("TODO")
        }

        // to test the property `imageName` - An alternate name for the image pushed to the repository. If this is empty, it will use the build name.  Can be used in conjunction with `image_tag` to direct multiple builds with different configs to push to the same image registry, under different, independantly versioned tags.
        should("test imageName") {
            // uncomment below to test the property
            //modelInstance.imageName shouldBe ("TODO")
        }

        // to test the property `imageRegistry` - Configuration for the registry/s to push the built image to. The first registry in this list will be used with attached Deployments.
        should("test imageRegistry") {
            // uncomment below to test the property
            //modelInstance.imageRegistry shouldBe ("TODO")
        }

        // to test the property `imageTag` - An extra tag put after the build version, for the image pushed to the repository. Eg. in image tag of `aarch64` would push to moghtech/komodo-core:1.13.2-aarch64. If this is empty, the image tag will just be the build version.  Can be used in conjunction with `image_name` to direct multiple builds with different configs to push to the same image registry, under different, independantly versioned tags.
        should("test imageTag") {
            // uncomment below to test the property
            //modelInstance.imageTag shouldBe ("TODO")
        }

        // to test the property `includeCommitTag` - Push commit hash `:a6v8h83` / `:a6v8h83-image_tag` tags.
        should("test includeCommitTag") {
            // uncomment below to test the property
            //modelInstance.includeCommitTag shouldBe ("TODO")
        }

        // to test the property `includeLatestTag` - Push `:latest` / `:latest-image_tag` tags.
        should("test includeLatestTag") {
            // uncomment below to test the property
            //modelInstance.includeLatestTag shouldBe ("TODO")
        }

        // to test the property `includeVersionTags` - Push build version semver `:1.19.5` + `1.19` / `:1.19.5-image_tag` tags.
        should("test includeVersionTags") {
            // uncomment below to test the property
            //modelInstance.includeVersionTags shouldBe ("TODO")
        }

        // to test the property `labels` - Docker labels
        should("test labels") {
            // uncomment below to test the property
            //modelInstance.labels shouldBe ("TODO")
        }

        // to test the property `linkedRepo` - Choose a Komodo Repo (Resource) to source the build files.
        should("test linkedRepo") {
            // uncomment below to test the property
            //modelInstance.linkedRepo shouldBe ("TODO")
        }

        // to test the property `links` - Configure quick links that are displayed in the resource header
        should("test links") {
            // uncomment below to test the property
            //modelInstance.links shouldBe ("TODO")
        }

        // to test the property `preBuild` - The optional command run after repo clone and before docker build.
        should("test preBuild") {
            // uncomment below to test the property
            //modelInstance.preBuild shouldBe ("TODO")
        }

        // to test the property `repo` - The repo used as the source of the build.
        should("test repo") {
            // uncomment below to test the property
            //modelInstance.repo shouldBe ("TODO")
        }

        // to test the property `secretArgs` - Secret arguments.  These values remain hidden in the final image by using docker secret mounts. See <https://docs.docker.com/build/building/secrets>.  The values can be used in RUN commands: ```sh RUN --mount=type=secret,id=SECRET_KEY \\   SECRET_KEY=$(cat /run/secrets/SECRET_KEY) ... ```
        should("test secretArgs") {
            // uncomment below to test the property
            //modelInstance.secretArgs shouldBe ("TODO")
        }

        // to test the property `skipSecretInterp` - Whether to skip secret interpolation in the build_args.
        should("test skipSecretInterp") {
            // uncomment below to test the property
            //modelInstance.skipSecretInterp shouldBe ("TODO")
        }

        // to test the property `useBuildx` - Whether to use buildx to build (eg `docker buildx build ...`)
        should("test useBuildx") {
            // uncomment below to test the property
            //modelInstance.useBuildx shouldBe ("TODO")
        }

        // to test the property `version` - The current version of the build.
        should("test version") {
            // uncomment below to test the property
            //modelInstance.version shouldBe ("TODO")
        }

        // to test the property `webhookEnabled` - Whether incoming webhooks actually trigger action.
        should("test webhookEnabled") {
            // uncomment below to test the property
            //modelInstance.webhookEnabled shouldBe ("TODO")
        }

        // to test the property `webhookSecret` - Optionally provide an alternate webhook secret for this build. If its an empty string, use the default secret from the config.
        should("test webhookSecret") {
            // uncomment below to test the property
            //modelInstance.webhookSecret shouldBe ("TODO")
        }

    }
}
