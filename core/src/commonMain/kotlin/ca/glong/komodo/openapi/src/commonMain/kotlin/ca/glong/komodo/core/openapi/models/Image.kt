/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models

import ca.glong.komodo.core.openapi.models.GraphDriverData
import ca.glong.komodo.core.openapi.models.ImageConfig
import ca.glong.komodo.core.openapi.models.ImageInspectMetadata
import ca.glong.komodo.core.openapi.models.ImageInspectRootFs
import ca.glong.komodo.core.openapi.models.ImageManifestSummary
import ca.glong.komodo.core.openapi.models.OciDescriptor

import kotlinx.serialization.*
import kotlinx.serialization.descriptors.*
import kotlinx.serialization.encoding.*

/**
 * Information about an image in the local image cache.
 *
 * @param architecture Hardware CPU architecture that the image runs on.
 * @param author Name of the author that was specified when committing the image, or as specified through MAINTAINER (deprecated) in the Dockerfile.
 * @param comment Optional message that was set when committing or importing the image.
 * @param config 
 * @param created Date and time at which the image was created, formatted in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.  This information is only available if present in the image, and omitted otherwise.
 * @param descriptor Descriptor is an OCI descriptor of the image target. In case of a multi-platform image, this descriptor points to the OCI index or a manifest list.  This field is only present if the daemon provides a multi-platform image store.  WARNING: This is experimental and may change at any time without any backward compatibility.
 * @param graphDriver 
 * @param id ID is the content-addressable ID of an image.  This identifier is a content-addressable digest calculated from the image's configuration (which includes the digests of layers used by the image).  Note that this digest differs from the `RepoDigests` below, which holds digests of image manifests that reference the image.
 * @param manifests Manifests is a list of image manifests available in this image. It provides a more detailed view of the platform-specific image manifests or other image-attached data like build attestations.  Only available if the daemon provides a multi-platform image store and the `manifests` option is set in the inspect request.  WARNING: This is experimental and may change at any time without any backward compatibility.
 * @param metadata 
 * @param os Operating System the image is built to run on.
 * @param osVersion Operating System version the image is built to run on (especially for Windows).
 * @param repoDigests List of content-addressable digests of locally available image manifests that the image is referenced from. Multiple manifests can refer to the same image.  These digests are usually only available if the image was either pulled from a registry, or if the image was pushed to a registry, which is when the manifest is generated and its digest calculated.
 * @param repoTags List of image names/tags in the local image cache that reference this image.  Multiple image tags can refer to the same image, and this list may be empty if no tags reference the image, in which case the image is \\\"untagged\\\", in which case it can still be referenced by its ID.
 * @param rootFS 
 * @param propertySize Total size of the image including all layers it is composed of.
 * @param variant CPU architecture variant (presently ARM-only).
 */
@Serializable@Serializable

data class Image (

    /* Hardware CPU architecture that the image runs on. */
    @SerialName(value = "Architecture") val architecture: kotlin.String? = null,

    /* Name of the author that was specified when committing the image, or as specified through MAINTAINER (deprecated) in the Dockerfile. */
    @SerialName(value = "Author") val author: kotlin.String? = null,

    /* Optional message that was set when committing or importing the image. */
    @SerialName(value = "Comment") val comment: kotlin.String? = null,

    @SerialName(value = "Config") val config: ImageConfig? = null,

    /* Date and time at which the image was created, formatted in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds.  This information is only available if present in the image, and omitted otherwise. */
    @SerialName(value = "Created") val created: kotlin.String? = null,

    /* Descriptor is an OCI descriptor of the image target. In case of a multi-platform image, this descriptor points to the OCI index or a manifest list.  This field is only present if the daemon provides a multi-platform image store.  WARNING: This is experimental and may change at any time without any backward compatibility. */
    @SerialName(value = "Descriptor") val descriptor: OciDescriptor? = null,

    @SerialName(value = "GraphDriver") val graphDriver: GraphDriverData? = null,

    /* ID is the content-addressable ID of an image.  This identifier is a content-addressable digest calculated from the image's configuration (which includes the digests of layers used by the image).  Note that this digest differs from the `RepoDigests` below, which holds digests of image manifests that reference the image. */
    @SerialName(value = "Id") val id: kotlin.String? = null,

    /* Manifests is a list of image manifests available in this image. It provides a more detailed view of the platform-specific image manifests or other image-attached data like build attestations.  Only available if the daemon provides a multi-platform image store and the `manifests` option is set in the inspect request.  WARNING: This is experimental and may change at any time without any backward compatibility. */
    @SerialName(value = "Manifests") val manifests: kotlin.collections.List<ImageManifestSummary>? = null,

    @SerialName(value = "Metadata") val metadata: ImageInspectMetadata? = null,

    /* Operating System the image is built to run on. */
    @SerialName(value = "Os") val os: kotlin.String? = null,

    /* Operating System version the image is built to run on (especially for Windows). */
    @SerialName(value = "OsVersion") val osVersion: kotlin.String? = null,

    /* List of content-addressable digests of locally available image manifests that the image is referenced from. Multiple manifests can refer to the same image.  These digests are usually only available if the image was either pulled from a registry, or if the image was pushed to a registry, which is when the manifest is generated and its digest calculated. */
    @SerialName(value = "RepoDigests") val repoDigests: kotlin.collections.List<kotlin.String>? = null,

    /* List of image names/tags in the local image cache that reference this image.  Multiple image tags can refer to the same image, and this list may be empty if no tags reference the image, in which case the image is \\\"untagged\\\", in which case it can still be referenced by its ID. */
    @SerialName(value = "RepoTags") val repoTags: kotlin.collections.List<kotlin.String>? = null,

    @SerialName(value = "RootFS") val rootFS: ImageInspectRootFs? = null,

    /* Total size of the image including all layers it is composed of. */
    @SerialName(value = "Size") val propertySize: kotlin.Long? = null,

    /* CPU architecture variant (presently ARM-only). */
    @SerialName(value = "Variant") val variant: kotlin.String? = null

) {


}

