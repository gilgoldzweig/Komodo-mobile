/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models


import kotlinx.serialization.*
import kotlinx.serialization.descriptors.*
import kotlinx.serialization.encoding.*

/**
 * The sync configuration.
 *
 * @param branch The branch of the repo.
 * @param commit Optionally set a specific commit hash.
 * @param delete Whether sync should delete resources not declared in the resource files
 * @param fileContents Manage the file contents in the UI.
 * @param filesOnHost Files are available on the Komodo Core host. Specify the file / folder with [ResourceSyncConfig::resource_path].
 * @param gitAccount The git account used to access private repos. Passing empty string can only clone public repos.  Note. A token for the account must be available in the core config or the builder server's periphery config for the configured git provider.
 * @param gitHttps Whether to use https to clone the repo (versus http). Default: true  Note. Komodo does not currently support cloning repos via ssh.
 * @param gitProvider The git provider domain. Default: github.com
 * @param includeResources Whether sync should include resources. Default: true
 * @param includeUserGroups Whether sync should include user groups.
 * @param includeVariables Whether sync should include variables.
 * @param linkedRepo Choose a Komodo Repo (Resource) to source the sync files.
 * @param managed Enable \"pushes\" to the file, which exports resources matching tags to single file.  - If using `files_on_host`, it is stored in the file_contents, which must point to a .toml file path (it will be created if it doesn't exist).  - If using `file_contents`, it is stored in the database. When using this, \"delete\" mode is always enabled.
 * @param matchTags When using `managed` resource sync, will only export resources matching all of the given tags. If none, will match all resources.
 * @param pendingAlert Whether sync should send alert when it enters Pending state. Default: true
 * @param repo The Github repo used as the source of the build.
 * @param resourcePath The path of the resource file(s) to sync.  - If Files on Host, this is relative to the configured `sync_directory` in core config.  - If Git Repo based, this is relative to the root of the repo. Can be a specific file, or a directory containing multiple files / folders. See [https://komo.do/docs/sync-resources](https://komo.do/docs/sync-resources) for more information.
 * @param webhookEnabled Whether incoming webhooks actually trigger action.
 * @param webhookSecret Optionally provide an alternate webhook secret for this sync. If its an empty string, use the default secret from the config.
 */
@Serializable@Serializable

data class ResourceSyncConfig (

    /* The branch of the repo. */
    @SerialName(value = "branch") val branch: kotlin.String? = null,

    /* Optionally set a specific commit hash. */
    @SerialName(value = "commit") val commit: kotlin.String? = null,

    /* Whether sync should delete resources not declared in the resource files */
    @SerialName(value = "delete") val delete: kotlin.Boolean? = null,

    /* Manage the file contents in the UI. */
    @SerialName(value = "file_contents") val fileContents: kotlin.String? = null,

    /* Files are available on the Komodo Core host. Specify the file / folder with [ResourceSyncConfig::resource_path]. */
    @SerialName(value = "files_on_host") val filesOnHost: kotlin.Boolean? = null,

    /* The git account used to access private repos. Passing empty string can only clone public repos.  Note. A token for the account must be available in the core config or the builder server's periphery config for the configured git provider. */
    @SerialName(value = "git_account") val gitAccount: kotlin.String? = null,

    /* Whether to use https to clone the repo (versus http). Default: true  Note. Komodo does not currently support cloning repos via ssh. */
    @SerialName(value = "git_https") val gitHttps: kotlin.Boolean? = null,

    /* The git provider domain. Default: github.com */
    @SerialName(value = "git_provider") val gitProvider: kotlin.String? = null,

    /* Whether sync should include resources. Default: true */
    @SerialName(value = "include_resources") val includeResources: kotlin.Boolean? = null,

    /* Whether sync should include user groups. */
    @SerialName(value = "include_user_groups") val includeUserGroups: kotlin.Boolean? = null,

    /* Whether sync should include variables. */
    @SerialName(value = "include_variables") val includeVariables: kotlin.Boolean? = null,

    /* Choose a Komodo Repo (Resource) to source the sync files. */
    @SerialName(value = "linked_repo") val linkedRepo: kotlin.String? = null,

    /* Enable \"pushes\" to the file, which exports resources matching tags to single file.  - If using `files_on_host`, it is stored in the file_contents, which must point to a .toml file path (it will be created if it doesn't exist).  - If using `file_contents`, it is stored in the database. When using this, \"delete\" mode is always enabled. */
    @SerialName(value = "managed") val managed: kotlin.Boolean? = null,

    /* When using `managed` resource sync, will only export resources matching all of the given tags. If none, will match all resources. */
    @SerialName(value = "match_tags") val matchTags: kotlin.collections.List<kotlin.String>? = null,

    /* Whether sync should send alert when it enters Pending state. Default: true */
    @SerialName(value = "pending_alert") val pendingAlert: kotlin.Boolean? = null,

    /* The Github repo used as the source of the build. */
    @SerialName(value = "repo") val repo: kotlin.String? = null,

    /* The path of the resource file(s) to sync.  - If Files on Host, this is relative to the configured `sync_directory` in core config.  - If Git Repo based, this is relative to the root of the repo. Can be a specific file, or a directory containing multiple files / folders. See [https://komo.do/docs/sync-resources](https://komo.do/docs/sync-resources) for more information. */
    @SerialName(value = "resource_path") val resourcePath: kotlin.collections.List<kotlin.String>? = null,

    /* Whether incoming webhooks actually trigger action. */
    @SerialName(value = "webhook_enabled") val webhookEnabled: kotlin.Boolean? = null,

    /* Optionally provide an alternate webhook secret for this sync. If its an empty string, use the default secret from the config. */
    @SerialName(value = "webhook_secret") val webhookSecret: kotlin.String? = null

) {


}

