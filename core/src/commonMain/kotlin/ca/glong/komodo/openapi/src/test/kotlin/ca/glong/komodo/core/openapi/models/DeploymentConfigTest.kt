/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models

import io.kotlintest.shouldBe
import io.kotlintest.specs.ShouldSpec

import ca.glong.komodo.core.openapi.models.DeploymentConfig
import ca.glong.komodo.core.openapi.models.DeploymentImage
import ca.glong.komodo.core.openapi.models.RestartMode
import ca.glong.komodo.core.openapi.models.TerminationSignal

class DeploymentConfigTest : ShouldSpec() {
    init {
        // uncomment below to create an instance of DeploymentConfig
        //val modelInstance = DeploymentConfig()

        // to test the property `autoUpdate` - Whether to automatically redeploy when newer a image is found. Will implicitly enable `poll_for_updates`, you don't need to enable both.
        should("test autoUpdate") {
            // uncomment below to test the property
            //modelInstance.autoUpdate shouldBe ("TODO")
        }

        // to test the property `command` - This is interpolated at the end of the `docker run` command, which means they are either passed to the containers inner process, or replaces the container command, depending on use of ENTRYPOINT or CMD in dockerfile. Empty is no command.
        should("test command") {
            // uncomment below to test the property
            //modelInstance.command shouldBe ("TODO")
        }

        // to test the property `environment` - The environment variables passed to the container / service.
        should("test environment") {
            // uncomment below to test the property
            //modelInstance.environment shouldBe ("TODO")
        }

        // to test the property `extraArgs` - Extra args which are interpolated into the `docker run` / `docker service create` command, and affect the container configuration.  - Container ref: https://docs.docker.com/reference/cli/docker/container/run/#options - Swarm Service ref: https://docs.docker.com/reference/cli/docker/service/create/#options
        should("test extraArgs") {
            // uncomment below to test the property
            //modelInstance.extraArgs shouldBe ("TODO")
        }

        // to test the property `image` - The image which the deployment deploys. Can either be a user inputted image, or a Komodo Build.
        should("test image") {
            // uncomment below to test the property
            //modelInstance.image shouldBe ("TODO")
        }

        // to test the property `imageRegistryAccount` - Configure the account used to pull the image from the registry. Used with `docker login`.   - If the field is empty string, will use the same account config as the build, or none at all if using image.  - If the field contains an account, a token for the account must be available.  - Will get the registry domain from the build / image
        should("test imageRegistryAccount") {
            // uncomment below to test the property
            //modelInstance.imageRegistryAccount shouldBe ("TODO")
        }

        // to test the property `labels` - The docker labels given to the container.
        should("test labels") {
            // uncomment below to test the property
            //modelInstance.labels shouldBe ("TODO")
        }

        // to test the property `links` - Configure quick links that are displayed in the resource header
        should("test links") {
            // uncomment below to test the property
            //modelInstance.links shouldBe ("TODO")
        }

        // to test the property `network` - The network attached to the container. Default is `host`.
        should("test network") {
            // uncomment below to test the property
            //modelInstance.network shouldBe ("TODO")
        }

        // to test the property `pollForUpdates` - Whether to poll for any updates to the image.
        should("test pollForUpdates") {
            // uncomment below to test the property
            //modelInstance.pollForUpdates shouldBe ("TODO")
        }

        // to test the property `ports` - The container port mapping. Irrelevant if container network is `host`. Maps ports on host to ports on container.
        should("test ports") {
            // uncomment below to test the property
            //modelInstance.ports shouldBe ("TODO")
        }

        // to test the property `redeployOnBuild` - Whether to redeploy the deployment whenever the attached build finishes.
        should("test redeployOnBuild") {
            // uncomment below to test the property
            //modelInstance.redeployOnBuild shouldBe ("TODO")
        }

        // to test the property `replicas` - The number of replicas for the Service.  Note. Only used in Swarm mode.
        should("test replicas") {
            // uncomment below to test the property
            //modelInstance.replicas shouldBe ("TODO")
        }

        // to test the property `restart` - The restart mode given to the container.
        should("test restart") {
            // uncomment below to test the property
            //modelInstance.restart shouldBe ("TODO")
        }

        // to test the property `sendAlerts` - Whether to send ContainerStateChange alerts for this deployment.
        should("test sendAlerts") {
            // uncomment below to test the property
            //modelInstance.sendAlerts shouldBe ("TODO")
        }

        // to test the property `serverId` - The Server to deploy the Deployment on, setting the Deployment into Container mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Deployment will be in Swarm mode.
        should("test serverId") {
            // uncomment below to test the property
            //modelInstance.serverId shouldBe ("TODO")
        }

        // to test the property `skipSecretInterp` - Whether to skip secret interpolation into the deployment environment variables.
        should("test skipSecretInterp") {
            // uncomment below to test the property
            //modelInstance.skipSecretInterp shouldBe ("TODO")
        }

        // to test the property `swarmId` - The Swarm to deploy the Deployment on (as a Swarm Service), setting the Deployment into Swarm mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Deployment will be in Swarm mode.
        should("test swarmId") {
            // uncomment below to test the property
            //modelInstance.swarmId shouldBe ("TODO")
        }

        // to test the property `termSignalLabels` - Labels attached to various termination signal options. Used to specify different shutdown functionality depending on the termination signal.
        should("test termSignalLabels") {
            // uncomment below to test the property
            //modelInstance.termSignalLabels shouldBe ("TODO")
        }

        // to test the property `terminationSignal` - The default termination signal to use to stop the deployment. Defaults to SigTerm (default docker signal).
        should("test terminationSignal") {
            // uncomment below to test the property
            //modelInstance.terminationSignal shouldBe ("TODO")
        }

        // to test the property `terminationTimeout` - The termination timeout.
        should("test terminationTimeout") {
            // uncomment below to test the property
            //modelInstance.terminationTimeout shouldBe ("TODO")
        }

        // to test the property `volumes` - The container volume mapping. Maps files / folders on host to files / folders in container.
        should("test volumes") {
            // uncomment below to test the property
            //modelInstance.volumes shouldBe ("TODO")
        }

    }
}
