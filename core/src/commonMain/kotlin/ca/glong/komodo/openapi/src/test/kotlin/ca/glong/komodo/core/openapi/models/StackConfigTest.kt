/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models

import io.kotlintest.shouldBe
import io.kotlintest.specs.ShouldSpec

import ca.glong.komodo.core.openapi.models.StackConfig
import ca.glong.komodo.core.openapi.models.AdditionalEnvFile
import ca.glong.komodo.core.openapi.models.StackFileDependency
import ca.glong.komodo.core.openapi.models.SystemCommand

class StackConfigTest : ShouldSpec() {
    init {
        // uncomment below to create an instance of StackConfig
        //val modelInstance = StackConfig()

        // to test the property `additionalEnvFiles` - Add additional env files to attach with `--env-file`. Relative to the run directory root.  Note. It is already included as an `additional_file`. Don't add it again there.
        should("test additionalEnvFiles") {
            // uncomment below to test the property
            //modelInstance.additionalEnvFiles shouldBe ("TODO")
        }

        // to test the property `autoPull` - Whether to automatically `compose pull` before redeploying stack. Ensured latest images are deployed. Will fail if the compose file specifies a locally build image.  Note. Not used in Swarm mode.
        should("test autoPull") {
            // uncomment below to test the property
            //modelInstance.autoPull shouldBe ("TODO")
        }

        // to test the property `autoUpdate` - Whether to automatically redeploy when newer images are found. Will implicitly enable `poll_for_updates`, you don't need to enable both.
        should("test autoUpdate") {
            // uncomment below to test the property
            //modelInstance.autoUpdate shouldBe ("TODO")
        }

        // to test the property `autoUpdateAllServices` - If auto update is enabled, Komodo will by default only update the specific services with image updates. If this parameter is set to true, Komodo will redeploy the whole Stack (all services).
        should("test autoUpdateAllServices") {
            // uncomment below to test the property
            //modelInstance.autoUpdateAllServices shouldBe ("TODO")
        }

        // to test the property `branch` - The branch of the repo.
        should("test branch") {
            // uncomment below to test the property
            //modelInstance.branch shouldBe ("TODO")
        }

        // to test the property `buildExtraArgs` - The extra arguments to pass after `docker compose build`. If empty, no extra build arguments will be passed. Only used if `run_build: true`  Note. Not used in Swarm mode.
        should("test buildExtraArgs") {
            // uncomment below to test the property
            //modelInstance.buildExtraArgs shouldBe ("TODO")
        }

        // to test the property `clonePath` - Optionally set a specific clone path
        should("test clonePath") {
            // uncomment below to test the property
            //modelInstance.clonePath shouldBe ("TODO")
        }

        // to test the property `commit` - Optionally set a specific commit hash.
        should("test commit") {
            // uncomment below to test the property
            //modelInstance.commit shouldBe ("TODO")
        }

        // to test the property `composeCmdWrapper` - Optional command wrapper for secrets management tools. Wraps the docker compose up command with a prefix command. Use [[COMPOSE_COMMAND]] as placeholder for the full compose command.  Examples: - \"op run -- [[COMPOSE_COMMAND]]\" (1password CLI) - \"sops exec-file --no-fifo /path/to/secret.env '[[COMPOSE_COMMAND]]'\" (sops)
        should("test composeCmdWrapper") {
            // uncomment below to test the property
            //modelInstance.composeCmdWrapper shouldBe ("TODO")
        }

        // to test the property `configFiles` - Add additional config files either in repo or on host to track. Can add any files associated with the stack to enable editing them in the UI. Doing so will also include diffing these when deciding to deploy in `DeployStackIfChanged`. Relative to the run directory.  Note. If the config file is .env and should be included in compose command using `--env-file`, add it to `additional_env_files` instead.
        should("test configFiles") {
            // uncomment below to test the property
            //modelInstance.configFiles shouldBe ("TODO")
        }

        // to test the property `destroyBeforeDeploy` - Whether to run `docker compose down` before `compose up`.
        should("test destroyBeforeDeploy") {
            // uncomment below to test the property
            //modelInstance.destroyBeforeDeploy shouldBe ("TODO")
        }

        // to test the property `envFilePath` - The name of the written environment file before `docker compose up`. Relative to the run directory root. Default: .env  Note. Not used in Swarm mode.
        should("test envFilePath") {
            // uncomment below to test the property
            //modelInstance.envFilePath shouldBe ("TODO")
        }

        // to test the property `environment` - The environment variables passed to the compose file. They will be written to path defined in env_file_path, which is given relative to the run directory.  If it is empty, no file will be written.  Note. Not used in Swarm mode.
        should("test environment") {
            // uncomment below to test the property
            //modelInstance.environment shouldBe ("TODO")
        }

        // to test the property `extraArgs` - The extra arguments to pass to the deploy command.  - For Compose stack, uses `docker compose up -d [EXTRA_ARGS]`. - For Swarm mode. `docker stack deploy [EXTRA_ARGS] STACK_NAME`  If empty, no extra arguments will be passed.
        should("test extraArgs") {
            // uncomment below to test the property
            //modelInstance.extraArgs shouldBe ("TODO")
        }

        // to test the property `fileContents` - The contents of the file directly, for management in the UI. If this is empty, it will fall back to checking git config for repo based compose file. Supports variable / secret interpolation.
        should("test fileContents") {
            // uncomment below to test the property
            //modelInstance.fileContents shouldBe ("TODO")
        }

        // to test the property `filePaths` - Add paths to compose files, relative to the run path. If this is empty, will use file `compose.yaml`.
        should("test filePaths") {
            // uncomment below to test the property
            //modelInstance.filePaths shouldBe ("TODO")
        }

        // to test the property `filesOnHost` - If this is checked, the stack will source the files on the host. Use `run_directory` and `file_paths` to specify the path on the host. This is useful for those who wish to setup their files on the host, rather than defining the contents in UI or in a git repo.
        should("test filesOnHost") {
            // uncomment below to test the property
            //modelInstance.filesOnHost shouldBe ("TODO")
        }

        // to test the property `gitAccount` - The git account used to access private repos. Passing empty string can only clone public repos.  Note. A token for the account must be available in the core config or the builder server's periphery config for the configured git provider.
        should("test gitAccount") {
            // uncomment below to test the property
            //modelInstance.gitAccount shouldBe ("TODO")
        }

        // to test the property `gitHttps` - Whether to use https to clone the repo (versus http). Default: true  Note. Komodo does not currently support cloning repos via ssh.
        should("test gitHttps") {
            // uncomment below to test the property
            //modelInstance.gitHttps shouldBe ("TODO")
        }

        // to test the property `gitProvider` - The git provider domain. Default: github.com
        should("test gitProvider") {
            // uncomment below to test the property
            //modelInstance.gitProvider shouldBe ("TODO")
        }

        // to test the property `ignoreServices` - Ignore certain services declared in the compose file when checking the stack status. For example, an init service might be exited, but the stack should be healthy. This init service should be in `ignore_services`
        should("test ignoreServices") {
            // uncomment below to test the property
            //modelInstance.ignoreServices shouldBe ("TODO")
        }

        // to test the property `linkedRepo` - Choose a Komodo Repo (Resource) to source the compose files.
        should("test linkedRepo") {
            // uncomment below to test the property
            //modelInstance.linkedRepo shouldBe ("TODO")
        }

        // to test the property `links` - Configure quick links that are displayed in the resource header
        should("test links") {
            // uncomment below to test the property
            //modelInstance.links shouldBe ("TODO")
        }

        // to test the property `pollForUpdates` - Whether to poll for any updates to the images.
        should("test pollForUpdates") {
            // uncomment below to test the property
            //modelInstance.pollForUpdates shouldBe ("TODO")
        }

        // to test the property `postDeploy` - The optional command to run after the Stack is deployed.
        should("test postDeploy") {
            // uncomment below to test the property
            //modelInstance.postDeploy shouldBe ("TODO")
        }

        // to test the property `preDeploy` - The optional command to run before the Stack is deployed.
        should("test preDeploy") {
            // uncomment below to test the property
            //modelInstance.preDeploy shouldBe ("TODO")
        }

        // to test the property `projectName` - Optionally specify a custom project name for the stack. If this is empty string, it will default to the stack name. Used with `docker compose -p {project_name}` / `docker stack deploy {project_name}`.  Note. Can be used to import pre-existing stacks with names that do not match Stack name.
        should("test projectName") {
            // uncomment below to test the property
            //modelInstance.projectName shouldBe ("TODO")
        }

        // to test the property `reclone` - By default, the Stack will `git pull` the repo after it is first cloned. If this option is enabled, the repo folder will be deleted and recloned instead.
        should("test reclone") {
            // uncomment below to test the property
            //modelInstance.reclone shouldBe ("TODO")
        }

        // to test the property `registryAccount` - Used with `registry_provider` to login to a registry before docker compose up.
        should("test registryAccount") {
            // uncomment below to test the property
            //modelInstance.registryAccount shouldBe ("TODO")
        }

        // to test the property `registryProvider` - Used with `registry_account` to login to a registry before docker compose up.
        should("test registryProvider") {
            // uncomment below to test the property
            //modelInstance.registryProvider shouldBe ("TODO")
        }

        // to test the property `repo` - The repo used as the source of the build. {namespace}/{repo_name}
        should("test repo") {
            // uncomment below to test the property
            //modelInstance.repo shouldBe ("TODO")
        }

        // to test the property `runBuild` - Whether to `docker compose build` before `compose down` / `compose up`. Combine with build_extra_args for custom behaviors.  Note. Not used in Swarm mode.
        should("test runBuild") {
            // uncomment below to test the property
            //modelInstance.runBuild shouldBe ("TODO")
        }

        // to test the property `runDirectory` - Directory to change to (`cd`) before running `docker compose up -d`.
        should("test runDirectory") {
            // uncomment below to test the property
            //modelInstance.runDirectory shouldBe ("TODO")
        }

        // to test the property `sendAlerts` - Whether to send StackStateChange alerts for this stack.
        should("test sendAlerts") {
            // uncomment below to test the property
            //modelInstance.sendAlerts shouldBe ("TODO")
        }

        // to test the property `serverId` - The Server to deploy the Stack on, setting the Stack into Compose mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Stack will be in Swarm mode.
        should("test serverId") {
            // uncomment below to test the property
            //modelInstance.serverId shouldBe ("TODO")
        }

        // to test the property `skipSecretInterp` - Whether to skip secret interpolation into the stack environment variables.
        should("test skipSecretInterp") {
            // uncomment below to test the property
            //modelInstance.skipSecretInterp shouldBe ("TODO")
        }

        // to test the property `swarmId` - The Swarm to deploy the Stack on, setting the Stack into Swarm mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Stack will be in Swarm mode.
        should("test swarmId") {
            // uncomment below to test the property
            //modelInstance.swarmId shouldBe ("TODO")
        }

        // to test the property `webhookEnabled` - Whether incoming webhooks actually trigger action.
        should("test webhookEnabled") {
            // uncomment below to test the property
            //modelInstance.webhookEnabled shouldBe ("TODO")
        }

        // to test the property `webhookForceDeploy` - By default, the Stack will `DeployStackIfChanged`. If this option is enabled, will always run `DeployStack` without diffing.
        should("test webhookForceDeploy") {
            // uncomment below to test the property
            //modelInstance.webhookForceDeploy shouldBe ("TODO")
        }

        // to test the property `webhookSecret` - Optionally provide an alternate webhook secret for this stack. If its an empty string, use the default secret from the config.
        should("test webhookSecret") {
            // uncomment below to test the property
            //modelInstance.webhookSecret shouldBe ("TODO")
        }

    }
}
