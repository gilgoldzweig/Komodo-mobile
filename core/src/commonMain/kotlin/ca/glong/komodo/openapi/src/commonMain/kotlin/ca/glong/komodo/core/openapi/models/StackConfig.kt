/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ca.glong.komodo.core.openapi.models

import ca.glong.komodo.core.openapi.models.AdditionalEnvFile
import ca.glong.komodo.core.openapi.models.StackFileDependency
import ca.glong.komodo.core.openapi.models.SystemCommand

import kotlinx.serialization.*
import kotlinx.serialization.descriptors.*
import kotlinx.serialization.encoding.*

/**
 * The compose file configuration.
 *
 * @param additionalEnvFiles Add additional env files to attach with `--env-file`. Relative to the run directory root.  Note. It is already included as an `additional_file`. Don't add it again there.
 * @param autoPull Whether to automatically `compose pull` before redeploying stack. Ensured latest images are deployed. Will fail if the compose file specifies a locally build image.  Note. Not used in Swarm mode.
 * @param autoUpdate Whether to automatically redeploy when newer images are found. Will implicitly enable `poll_for_updates`, you don't need to enable both.
 * @param autoUpdateAllServices If auto update is enabled, Komodo will by default only update the specific services with image updates. If this parameter is set to true, Komodo will redeploy the whole Stack (all services).
 * @param branch The branch of the repo.
 * @param buildExtraArgs The extra arguments to pass after `docker compose build`. If empty, no extra build arguments will be passed. Only used if `run_build: true`  Note. Not used in Swarm mode.
 * @param clonePath Optionally set a specific clone path
 * @param commit Optionally set a specific commit hash.
 * @param composeCmdWrapper Optional command wrapper for secrets management tools. Wraps the docker compose up command with a prefix command. Use [[COMPOSE_COMMAND]] as placeholder for the full compose command.  Examples: - \"op run -- [[COMPOSE_COMMAND]]\" (1password CLI) - \"sops exec-file --no-fifo /path/to/secret.env '[[COMPOSE_COMMAND]]'\" (sops)
 * @param configFiles Add additional config files either in repo or on host to track. Can add any files associated with the stack to enable editing them in the UI. Doing so will also include diffing these when deciding to deploy in `DeployStackIfChanged`. Relative to the run directory.  Note. If the config file is .env and should be included in compose command using `--env-file`, add it to `additional_env_files` instead.
 * @param destroyBeforeDeploy Whether to run `docker compose down` before `compose up`.
 * @param envFilePath The name of the written environment file before `docker compose up`. Relative to the run directory root. Default: .env  Note. Not used in Swarm mode.
 * @param environment The environment variables passed to the compose file. They will be written to path defined in env_file_path, which is given relative to the run directory.  If it is empty, no file will be written.  Note. Not used in Swarm mode.
 * @param extraArgs The extra arguments to pass to the deploy command.  - For Compose stack, uses `docker compose up -d [EXTRA_ARGS]`. - For Swarm mode. `docker stack deploy [EXTRA_ARGS] STACK_NAME`  If empty, no extra arguments will be passed.
 * @param fileContents The contents of the file directly, for management in the UI. If this is empty, it will fall back to checking git config for repo based compose file. Supports variable / secret interpolation.
 * @param filePaths Add paths to compose files, relative to the run path. If this is empty, will use file `compose.yaml`.
 * @param filesOnHost If this is checked, the stack will source the files on the host. Use `run_directory` and `file_paths` to specify the path on the host. This is useful for those who wish to setup their files on the host, rather than defining the contents in UI or in a git repo.
 * @param gitAccount The git account used to access private repos. Passing empty string can only clone public repos.  Note. A token for the account must be available in the core config or the builder server's periphery config for the configured git provider.
 * @param gitHttps Whether to use https to clone the repo (versus http). Default: true  Note. Komodo does not currently support cloning repos via ssh.
 * @param gitProvider The git provider domain. Default: github.com
 * @param ignoreServices Ignore certain services declared in the compose file when checking the stack status. For example, an init service might be exited, but the stack should be healthy. This init service should be in `ignore_services`
 * @param linkedRepo Choose a Komodo Repo (Resource) to source the compose files.
 * @param links Configure quick links that are displayed in the resource header
 * @param pollForUpdates Whether to poll for any updates to the images.
 * @param postDeploy The optional command to run after the Stack is deployed.
 * @param preDeploy The optional command to run before the Stack is deployed.
 * @param projectName Optionally specify a custom project name for the stack. If this is empty string, it will default to the stack name. Used with `docker compose -p {project_name}` / `docker stack deploy {project_name}`.  Note. Can be used to import pre-existing stacks with names that do not match Stack name.
 * @param reclone By default, the Stack will `git pull` the repo after it is first cloned. If this option is enabled, the repo folder will be deleted and recloned instead.
 * @param registryAccount Used with `registry_provider` to login to a registry before docker compose up.
 * @param registryProvider Used with `registry_account` to login to a registry before docker compose up.
 * @param repo The repo used as the source of the build. {namespace}/{repo_name}
 * @param runBuild Whether to `docker compose build` before `compose down` / `compose up`. Combine with build_extra_args for custom behaviors.  Note. Not used in Swarm mode.
 * @param runDirectory Directory to change to (`cd`) before running `docker compose up -d`.
 * @param sendAlerts Whether to send StackStateChange alerts for this stack.
 * @param serverId The Server to deploy the Stack on, setting the Stack into Compose mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Stack will be in Swarm mode.
 * @param skipSecretInterp Whether to skip secret interpolation into the stack environment variables.
 * @param swarmId The Swarm to deploy the Stack on, setting the Stack into Swarm mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Stack will be in Swarm mode.
 * @param webhookEnabled Whether incoming webhooks actually trigger action.
 * @param webhookForceDeploy By default, the Stack will `DeployStackIfChanged`. If this option is enabled, will always run `DeployStack` without diffing.
 * @param webhookSecret Optionally provide an alternate webhook secret for this stack. If its an empty string, use the default secret from the config.
 */
@Serializable@Serializable

data class StackConfig (

    /* Add additional env files to attach with `--env-file`. Relative to the run directory root.  Note. It is already included as an `additional_file`. Don't add it again there. */
    @SerialName(value = "additional_env_files") val additionalEnvFiles: kotlin.collections.List<AdditionalEnvFile>? = null,

    /* Whether to automatically `compose pull` before redeploying stack. Ensured latest images are deployed. Will fail if the compose file specifies a locally build image.  Note. Not used in Swarm mode. */
    @SerialName(value = "auto_pull") val autoPull: kotlin.Boolean? = null,

    /* Whether to automatically redeploy when newer images are found. Will implicitly enable `poll_for_updates`, you don't need to enable both. */
    @SerialName(value = "auto_update") val autoUpdate: kotlin.Boolean? = null,

    /* If auto update is enabled, Komodo will by default only update the specific services with image updates. If this parameter is set to true, Komodo will redeploy the whole Stack (all services). */
    @SerialName(value = "auto_update_all_services") val autoUpdateAllServices: kotlin.Boolean? = null,

    /* The branch of the repo. */
    @SerialName(value = "branch") val branch: kotlin.String? = null,

    /* The extra arguments to pass after `docker compose build`. If empty, no extra build arguments will be passed. Only used if `run_build: true`  Note. Not used in Swarm mode. */
    @SerialName(value = "build_extra_args") val buildExtraArgs: kotlin.collections.List<kotlin.String>? = null,

    /* Optionally set a specific clone path */
    @SerialName(value = "clone_path") val clonePath: kotlin.String? = null,

    /* Optionally set a specific commit hash. */
    @SerialName(value = "commit") val commit: kotlin.String? = null,

    /* Optional command wrapper for secrets management tools. Wraps the docker compose up command with a prefix command. Use [[COMPOSE_COMMAND]] as placeholder for the full compose command.  Examples: - \"op run -- [[COMPOSE_COMMAND]]\" (1password CLI) - \"sops exec-file --no-fifo /path/to/secret.env '[[COMPOSE_COMMAND]]'\" (sops) */
    @SerialName(value = "compose_cmd_wrapper") val composeCmdWrapper: kotlin.String? = null,

    /* Add additional config files either in repo or on host to track. Can add any files associated with the stack to enable editing them in the UI. Doing so will also include diffing these when deciding to deploy in `DeployStackIfChanged`. Relative to the run directory.  Note. If the config file is .env and should be included in compose command using `--env-file`, add it to `additional_env_files` instead. */
    @SerialName(value = "config_files") val configFiles: kotlin.collections.List<StackFileDependency>? = null,

    /* Whether to run `docker compose down` before `compose up`. */
    @SerialName(value = "destroy_before_deploy") val destroyBeforeDeploy: kotlin.Boolean? = null,

    /* The name of the written environment file before `docker compose up`. Relative to the run directory root. Default: .env  Note. Not used in Swarm mode. */
    @SerialName(value = "env_file_path") val envFilePath: kotlin.String? = null,

    /* The environment variables passed to the compose file. They will be written to path defined in env_file_path, which is given relative to the run directory.  If it is empty, no file will be written.  Note. Not used in Swarm mode. */
    @SerialName(value = "environment") val environment: kotlin.String? = null,

    /* The extra arguments to pass to the deploy command.  - For Compose stack, uses `docker compose up -d [EXTRA_ARGS]`. - For Swarm mode. `docker stack deploy [EXTRA_ARGS] STACK_NAME`  If empty, no extra arguments will be passed. */
    @SerialName(value = "extra_args") val extraArgs: kotlin.collections.List<kotlin.String>? = null,

    /* The contents of the file directly, for management in the UI. If this is empty, it will fall back to checking git config for repo based compose file. Supports variable / secret interpolation. */
    @SerialName(value = "file_contents") val fileContents: kotlin.String? = null,

    /* Add paths to compose files, relative to the run path. If this is empty, will use file `compose.yaml`. */
    @SerialName(value = "file_paths") val filePaths: kotlin.collections.List<kotlin.String>? = null,

    /* If this is checked, the stack will source the files on the host. Use `run_directory` and `file_paths` to specify the path on the host. This is useful for those who wish to setup their files on the host, rather than defining the contents in UI or in a git repo. */
    @SerialName(value = "files_on_host") val filesOnHost: kotlin.Boolean? = null,

    /* The git account used to access private repos. Passing empty string can only clone public repos.  Note. A token for the account must be available in the core config or the builder server's periphery config for the configured git provider. */
    @SerialName(value = "git_account") val gitAccount: kotlin.String? = null,

    /* Whether to use https to clone the repo (versus http). Default: true  Note. Komodo does not currently support cloning repos via ssh. */
    @SerialName(value = "git_https") val gitHttps: kotlin.Boolean? = null,

    /* The git provider domain. Default: github.com */
    @SerialName(value = "git_provider") val gitProvider: kotlin.String? = null,

    /* Ignore certain services declared in the compose file when checking the stack status. For example, an init service might be exited, but the stack should be healthy. This init service should be in `ignore_services` */
    @SerialName(value = "ignore_services") val ignoreServices: kotlin.collections.List<kotlin.String>? = null,

    /* Choose a Komodo Repo (Resource) to source the compose files. */
    @SerialName(value = "linked_repo") val linkedRepo: kotlin.String? = null,

    /* Configure quick links that are displayed in the resource header */
    @SerialName(value = "links") val links: kotlin.collections.List<kotlin.String>? = null,

    /* Whether to poll for any updates to the images. */
    @SerialName(value = "poll_for_updates") val pollForUpdates: kotlin.Boolean? = null,

    /* The optional command to run after the Stack is deployed. */
    @SerialName(value = "post_deploy") val postDeploy: SystemCommand? = null,

    /* The optional command to run before the Stack is deployed. */
    @SerialName(value = "pre_deploy") val preDeploy: SystemCommand? = null,

    /* Optionally specify a custom project name for the stack. If this is empty string, it will default to the stack name. Used with `docker compose -p {project_name}` / `docker stack deploy {project_name}`.  Note. Can be used to import pre-existing stacks with names that do not match Stack name. */
    @SerialName(value = "project_name") val projectName: kotlin.String? = null,

    /* By default, the Stack will `git pull` the repo after it is first cloned. If this option is enabled, the repo folder will be deleted and recloned instead. */
    @SerialName(value = "reclone") val reclone: kotlin.Boolean? = null,

    /* Used with `registry_provider` to login to a registry before docker compose up. */
    @SerialName(value = "registry_account") val registryAccount: kotlin.String? = null,

    /* Used with `registry_account` to login to a registry before docker compose up. */
    @SerialName(value = "registry_provider") val registryProvider: kotlin.String? = null,

    /* The repo used as the source of the build. {namespace}/{repo_name} */
    @SerialName(value = "repo") val repo: kotlin.String? = null,

    /* Whether to `docker compose build` before `compose down` / `compose up`. Combine with build_extra_args for custom behaviors.  Note. Not used in Swarm mode. */
    @SerialName(value = "run_build") val runBuild: kotlin.Boolean? = null,

    /* Directory to change to (`cd`) before running `docker compose up -d`. */
    @SerialName(value = "run_directory") val runDirectory: kotlin.String? = null,

    /* Whether to send StackStateChange alerts for this stack. */
    @SerialName(value = "send_alerts") val sendAlerts: kotlin.Boolean? = null,

    /* The Server to deploy the Stack on, setting the Stack into Compose mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Stack will be in Swarm mode. */
    @SerialName(value = "server_id") val serverId: kotlin.String? = null,

    /* Whether to skip secret interpolation into the stack environment variables. */
    @SerialName(value = "skip_secret_interp") val skipSecretInterp: kotlin.Boolean? = null,

    /* The Swarm to deploy the Stack on, setting the Stack into Swarm mode.  Note. If both swarm_id and server_id are set, swarm_id overrides server_id and the Stack will be in Swarm mode. */
    @SerialName(value = "swarm_id") val swarmId: kotlin.String? = null,

    /* Whether incoming webhooks actually trigger action. */
    @SerialName(value = "webhook_enabled") val webhookEnabled: kotlin.Boolean? = null,

    /* By default, the Stack will `DeployStackIfChanged`. If this option is enabled, will always run `DeployStack` without diffing. */
    @SerialName(value = "webhook_force_deploy") val webhookForceDeploy: kotlin.Boolean? = null,

    /* Optionally provide an alternate webhook secret for this stack. If its an empty string, use the default secret from the config. */
    @SerialName(value = "webhook_secret") val webhookSecret: kotlin.String? = null

) {


}

